<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gilang.dailynotes10120071">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DailyNotes10120071">
        <activity android:name=".main.profile.ProfileActivity" />
        <activity android:name=".main.diary.UpdateActivity" />
        <activity android:name=".main.diary.NotesActivity" />
        <activity android:name=".main.diary.ListNoteActivity" />
        <activity android:name=".main.diary.SaveCategoryActivity" />
        <activity android:name=".main.diary.CategoriActivity" />
        <activity android:name=".main.auth.RegisterActivity" />
        <activity android:name=".main.auth.LoginActivity" />
        <activity
            android:name=".main.viewpager.ViewPagerActivity"
            android:theme="@style/SplashScreenStyle"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>